export const CameraShakeConfig = {
    kill: {
        /** 击杀震动持续时间（秒）。 */
        duration: 0.2,
        /** 相机最大随机偏移（相机本地坐标）。 */
        strength: 12,
        /** 每秒更新随机偏移的次数。 */
        frequency: 30,
    },
};
